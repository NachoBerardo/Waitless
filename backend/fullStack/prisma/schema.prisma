generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

model Food {
  idFood      Int    @id @default(autoincrement())
  category    String
  name        String
  price       Int
  description String
  image       String 
  video       String 
}

//model Order {
  //id Int @id @default(autoincrement())
  //sendedAt DateTime @default(now())
  //idFood tabla de relacion
//}


model Mozo{
  idMozo Int @id @default(autoincrement())
  nombreMozo String 
}

